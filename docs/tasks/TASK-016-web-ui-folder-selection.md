# TASK-016 Web UI folder selection

## Status
in-progress

## Description
Implement UC-16 in the web UI: list import folders and show file previews so users can choose folders without CLI paths.

## Acceptance criteria
- [ ] Web page lists folders under bankImports/
- [ ] Each folder shows file count and basic metadata
- [ ] User can select one or more folders for next step

## Completion
**Date:** YYYY-MM-DD
**Summary:** 
**Files changed:**
- `path/to/file` -- created / modified
**Branch:** `git checkout -b task/016-web-ui-folder-selection`
**Stage:** `git add path/to/file1 path/to/file2`
**Commit:** `git commit -m "Implement web UI folder selection"`
