# TASK-021 Web UI Firefly settings

## Status
in-progress

## Description
Implement UC-15 in the web UI: configure Firefly URL and token from settings page with validation and persistence.

## Acceptance criteria
- [ ] UI form saves URL and token
- [ ] API validates URL against Firefly
- [ ] Values persist to config/secrets files

## Completion
**Date:** YYYY-MM-DD
**Summary:**
**Files changed:**
- `path/to/file` -- created / modified
**Branch:** `git checkout -b task/021-web-ui-settings-firefly-config`
**Stage:** `git add path/to/file1 path/to/file2`
**Commit:** `git commit -m "Implement web UI Firefly settings"`
