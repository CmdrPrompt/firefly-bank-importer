# TASK-047 Expand firefly-python-client with account, transaction, and resource methods

## Status
obsolete

## Description

Superseded by TASK-046. All API methods described here (`get_asset_accounts`,
`get_latest_transaction_date`, `create_transaction`, `get_bills`, `get_budgets`,
`get_budget_limits`, `get_categories`, `get_summary`) were implemented in
`firefly-python-api` as part of TASK-002 and TASK-003 in that repository.

The integration work (replacing inline calls in `import_firefly.py` and
`web_ui.py`) is covered by the updated TASK-046.
