# TASK-017 Web UI account matching

## Status
todo

## Description
Implement UC-17 in the web UI: allow interactive mapping between selected folders and Firefly accounts.

## Acceptance criteria
- [ ] API provides account candidates for each folder
- [ ] UI allows selecting/overriding account mapping per folder
- [ ] Mapping validation prevents continuing with unresolved folders

## Completion
**Date:** YYYY-MM-DD
**Summary:** 
**Files changed:**
- `path/to/file` -- created / modified
**Branch:** `git checkout -b task/017-web-ui-account-matching`
**Stage:** `git add path/to/file1 path/to/file2`
**Commit:** `git commit -m "Implement web UI account matching"`
